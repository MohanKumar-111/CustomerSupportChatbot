# Customer Support Chatbot

## Features
- Uses OpenAI for complex queries
- Uses Groq as a fallback if OpenAI fails
- Maintains conversation memory
- Stores chat history in JSON
- Logs all user queries and AI responses

## Run

pip install -r requirements.txt

python main.py
